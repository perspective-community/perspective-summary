import type { TableReadFormat } from "./TableReadFormat.js";
/**
 * Options for [`Table::update`].
 */
export type UpdateOptions = {
    port_id: number | null;
    format: TableReadFormat | null;
};
